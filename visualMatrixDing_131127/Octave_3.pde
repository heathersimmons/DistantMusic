if (pitch==61) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(26, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(26, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
  if (pitch==62) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(27, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(27, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
  if (pitch==63) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(28, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(28, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
  if (pitch==64) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(29, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(29, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
  if (pitch==65) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(30, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(30, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
  if (pitch==66) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(31, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(31, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
  if (pitch==67) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(32, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(32, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
   
  if (pitch==68) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(33, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(33, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
   
  if (pitch==69) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(34, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(34, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
   
  if (pitch==70) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(35, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(35, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
   
  if (pitch==71) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(36, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(36, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
   
  if (pitch==72) {
    note5=!note5;
    cp5.get(Matrix.class, "Piano").set(37, 0, false);
    myBus.sendNoteOn(channel, pitch, velocity); 
    if (note5)
    {
      cp5.get(Matrix.class, "Piano").set(37, 0, true);
      myBus.sendNoteOn(channel, pitch, velocity); // Send a Midi noteOn
      delay(notedelay);
      myBus.sendNoteOff(channel, pitch, velocity); // Send a Midi nodeOf
    }
  }
